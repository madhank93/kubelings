---
kind: module

title: "Node & Control Plane — failures a container can't stage"

description: |
  The faults that live below kubectl: a kubelet or containerd that won't start,
  a crash-looped kube-apiserver, an etcd NOSPACE alarm to compact and defrag,
  static pods, pending node CSRs, a cgroup-driver mismatch, clock skew, and the
  kernel sysctls pod networking needs. Every drill needs a real machine with
  systemd, a kernel, and root — so these run on iximiuz Labs only, never local
  kind.

name: module-11

createdAt: 2026-07-23
updatedAt: 2026-07-23
---
