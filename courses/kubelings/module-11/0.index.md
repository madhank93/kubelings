---
kind: module

title: "Node & Control Plane — failures a container can't stage"

description: |
  The faults that live below kubectl: a kubelet or containerd that won't start,
  a kube-apiserver you crash-looped with a bad flag, an etcd NOSPACE alarm you
  compact and defrag, a static pod you author by hand, pending node CSRs you
  approve, a cgroup-driver mismatch that bricks a node, clock skew that breaks
  TLS, and a missing kernel module that kills pod networking. Every drill needs
  a real machine with systemd, a kernel, and root — so these run on iximiuz
  Labs only, never on your local kind cluster.

name: module-11

createdAt: 2026-07-23
updatedAt: 2026-07-23
---
