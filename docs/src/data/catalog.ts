// src/data/catalog.ts
// AUTO-DERIVED from courses/kubelings/ (the source of truth). 107 lessons.
// Regenerate when lessons change; do not hand-edit entries.

export type CatalogEntry = {
  module: string;
  slug: string;
  scenario: string;
  type: 'lab' | 'incident' | 'drill' | 'read';
  iximiuz: boolean;
  kind: boolean;
  handsOn: boolean;
  description: string;
  real: boolean;              // reproduces a cited, real company incident
  company?: string;
  source?: string;            // link to the public postmortem
  writeUp?: string;           // src/data/incidents/<name>.md, rendered in the modal
  detail: boolean;            // has src/data/lesson-details/<slug>.md
};

export const MODULES: Record<string, { label: string; color: string; learn: string }> = {
  M1:  { label: "Foundations", color: '#7c6af5', learn: "pods, Deployments, Services, namespaces, labels & selectors, the triage loop (`describe` → `logs` → fix → watch)" },
  M2:  { label: "Workloads", color: '#3b9eff', learn: "rolling updates, blue/green & canary, DaemonSets, StatefulSets, Jobs, CronJobs, HPA, OOMKill & right-sizing, CPU throttling, probes, init containers, PDBs, QoS, ephemeral-container debugging, multi-container patterns, readiness/CronJob/rollout failure drills, VPA, KEDA" },
  M3:  { label: "Config & Storage", color: '#d29922', learn: "ConfigMaps, Secrets, PV/PVC lifecycle, StorageClasses, access modes, finalizer traps, kustomize, Helm release lifecycle, ghost-endpoint & secret-rotation & stuck-namespace drills" },
  M4:  { label: "Networking", color: '#4fa86d', learn: "Services & endpoints, Ingress & Gateway API, NetworkPolicy, CoreDNS & the ndots amplifier, kube-proxy dataplane, CNI anatomy & triage, conntrack, graceful shutdown, kubeconfig contexts" },
  M5:  { label: "Scheduling & Placement", color: '#9b5de5', learn: "affinity/anti-affinity, taints & tolerations, topology spread, priority & preemption, noisy neighbors" },
  M6:  { label: "Security", color: '#c53030', learn: "RBAC, ServiceAccounts & tokens, Pod Security, admission webhooks, container hardening, CIS benchmarks, egress lockdown, image digests, Gatekeeper & Kyverno policy engines, trivy scanning, cosign signatures & SBOMs, seccomp/AppArmor, encryption-at-rest, audit policy, Falco runtime detection" },
  M7:  { label: "Internals", color: '#e36f0e', learn: "API server request & admission flow, watch/informers & APF, etcd (incl. backup/restore), CRDs & building operators, scheduler internals, controller reconciliation, kubelet ↔ CRI, leader election, kubeadm bootstrap, HA control planes, certificate rotation" },
  M8:  { label: "Observability & SRE", color: '#1f8a9c', learn: "events forensics, node NotReady triage, quotas, disk pressure & eviction, cluster upgrades, node maintenance, SLO burn-rate alerting, OTel tracing pipelines, debugging playbooks" },
  M9:  { label: "War Stories", color: '#e85d9f', learn: "multi-concept cascade incidents from cited postmortems — everything at once, then the final boss" },
  M10: { label: "Platform Engineering", color: '#db6d28', learn: "GitOps with Argo CD (incl. app-of-apps) and Flux, multi-tenancy with Capsule, Cluster API, Crossplane compositions" },
};

export const CATALOG: CatalogEntry[] = [
  // ── M1 Foundations ──────
  { module:"M1", slug:"welcome", scenario:"Welcome to Kubelings", type:"read", iximiuz:false, kind:false, handsOn:false, description:"How the course works: read the situation, fix the live cluster, watch the automated check turn green.", real:false, detail:true },
  { module:"M1", slug:"crashloop-triage", scenario:"CrashLoopBackOff: read the logs before you guess", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The new `orders` service won't stay up — pods flash into Running, die within a second, and settle into CrashLoopBackOff. Nothing is wrong with the image. Learn the triage loop every later lesson builds on: describe → logs → fix → watch.", real:false, detail:true },
  { module:"M1", slug:"expose-web", scenario:"Expose a Deployment: your first Service", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The `web` frontend is running — two healthy nginx pods — but nothing in the cluster can reach it: there's no Service. Create one, watch endpoints populate, and learn how label selectors stitch traffic to pods.", real:false, detail:true },
  { module:"M1", slug:"selector-mismatch", scenario:"The Service that routes to nothing", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The `api` Service exists, DNS resolves, the pods are healthy — and every request times out. Its endpoints list is empty: the selector matches zero pods. One character is wrong somewhere. Find it.", real:false, detail:true },
  { module:"M1", slug:"namespace-basics", scenario:"Namespaces: walls, names, and crossing them", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The platform team wants a `staging` environment isolated from everything else, and the app config copied into it. Learn what namespaces isolate (names, RBAC, quotas) and what they don't (network, nodes) — and how DNS crosses the wall.", real:false, detail:true },
  { module:"M1", slug:"imagepull-backoff", scenario:"ImagePullBackOff: the tag that never existed", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A hotfix deploy is stuck — pods sit in ImagePullBackOff and the rollout never completes. The registry is fine. The image is fine. The tag is not. Learn to read pull errors and roll the Deployment back to health.", real:false, detail:true },
  { module:"M1", slug:"kubectl-detective", scenario:"kubectl detective: find the broken one", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Five microservices deployed, dashboards say \"one of them is down\", and that's all anyone knows. No names, no hints. Use get/describe/events to find the odd one out and bring it back. Speed comes from method, not luck.", real:false, detail:true },
  // ── M2 Workloads ──────
  { module:"M2", slug:"rolling-update", scenario:"Fix the Rolling Update: unsafe maxSurge/maxUnavailable", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A web Deployment ships every release with a full outage — its rollout strategy tears down all pods at once and never surges new ones. Fix the rolling update strategy so deploys stay available, then confirm the Deployment is healthy.", real:false, detail:true },
  { module:"M2", slug:"daemonset", scenario:"Build a Node-Level Log Collector DaemonSet", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Ops needs a log shipper running on every node — exactly one pod per node, now and on any node added later. Build a DaemonSet that satisfies this and confirm it lands a Ready pod on each node.", real:false, detail:true },
  { module:"M2", slug:"statefulset", scenario:"StatefulSet with Stable Pod Identity + Headless Service", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A stateful app needs stable, predictable pod names and per-pod DNS. Build a StatefulSet fronted by a headless Service so each replica gets a durable network identity, then confirm all replicas are Ready.", real:false, detail:true },
  { module:"M2", slug:"jobs", scenario:"The Job That Never Finishes", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A one-shot data-import Job runs forever and never reports completion, so the pipeline that waits on it is stuck. Diagnose why, then make the Job actually complete.", real:false, detail:true },
  { module:"M2", slug:"cronjobs", scenario:"CronJob Pileup: fix the concurrencyPolicy", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A every-minute CronJob runs longer than a minute, so overlapping runs stack up and exhaust resources. Configure the CronJob so a new run never starts while the previous one is still going.", real:false, detail:true },
  { module:"M2", slug:"hpa", scenario:"Autoscale a Deployment with an HPA (1 → 5)", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A CPU-bound web app needs to scale out under load and back in when idle. With metrics-server already running, create a HorizontalPodAutoscaler that scales the Deployment between 1 and 5 replicas on CPU.", real:false, detail:true },
  { module:"M2", slug:"oomkill", scenario:"OOMKilled CrashLoop: right-size the memory limit", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A cache Deployment is stuck in CrashLoopBackOff — every pod is OOMKilled seconds after start. Diagnose the resource misconfiguration and give the workload enough memory to run.", real:false, detail:true },
  { module:"M2", slug:"liveness-vs-readiness", scenario:"The liveness probe that kills healthy pods", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Someone \"added health checks\" to the payments service and now it restarts forever — the liveness probe checks the wrong port, so the kubelet executes a perfectly healthy container every few seconds. Learn what liveness and readiness actually promise, and when each one should fail.", real:false, detail:true },
  { module:"M2", slug:"startup-probe", scenario:"Slow starter vs impatient liveness", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A legacy service needs ~40 seconds to warm up. Its liveness probe gives it 10. The pod is killed mid-boot, restarts, gets killed again — it will literally never finish starting. Fix it the modern way: a startupProbe that holds liveness fire until boot completes.", real:false, detail:true },
  { module:"M2", slug:"init-containers", scenario:"Stuck at Init: the gate that never opens", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The `reports` pod has been Init:0/1 for twenty minutes. Its init container is waiting for configuration that will never arrive — it's checking the wrong name. Learn how init containers gate pod startup, and how to debug the gate.", real:false, detail:true },
  { module:"M2", slug:"pdb-blocks-drain", scenario:"The drain that never finishes", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Node maintenance tonight. But `kubectl drain` has been \"evicting\" the same two pods for ten minutes — a PodDisruptionBudget demands more availability than the deployment has replicas. Learn the PDB math and fix it so maintenance and availability can both be true.", real:false, detail:true },
  { module:"M2", slug:"qos-classes", scenario:"QoS classes: who gets killed first", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Three tiers of workload, one node under memory pressure — and the kernel needs someone to die. Kubernetes decides the order via QoS classes computed from your requests/limits. Make the payment service Guaranteed, the batch job honest, and learn to read .status.qosClass before the OOM killer reads it for you.", real:false, detail:true },
  { module:"M2", slug:"blue-green-canary", scenario:"Blue/green: the deploy you can undo in one second", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Rolling updates replace pods gradually — great until a bad release must be yanked *now*. Blue/green runs old and new side by side and flips traffic with one selector change. The green stack is up, tested, and receiving zero traffic; flip the switch without dropping a request, and keep the way back.", real:false, detail:true },
  { module:"M2", slug:"incident-cpu-throttling", scenario:"CPU throttling: the latency tax nobody sees", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"The service is \"healthy\" — CPU usage looks modest, no restarts, no errors — and p95 latency is garbage. The kernel is pausing your process dozens of times a second to enforce a CPU limit. Read the throttling counters the kernel keeps inside every container, then fix the limit like Omio and Buffer had to.", real:true, company:"Omio", source:"https://medium.com/omio-engineering/cpu-limits-and-aggressive-throttling-in-kubernetes-c5b20bd8a718", detail:true },
  { module:"M2", slug:"ephemeral-containers", scenario:"No shell, no exec, no problem", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A distroless production pod is misbehaving and `kubectl exec` greets you with \"no such file or directory\" — there is no shell in the image. Attach an ephemeral debug container with `kubectl debug`, read the app's filesystem through the shared PID namespace, and fix the bad config it's choking on.", real:false, detail:true },
  { module:"M2", slug:"multi-container-patterns", scenario:"Three pods, three broken patterns", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Sidecar, ambassador, adapter — the three classic multi-container patterns, each deployed here with one bug: a log-forwarder tailing the wrong path, a proxy pointed at the wrong port, an adapter writing to the wrong place. Fix all three and prove each pattern actually does its job.", real:false, detail:true },
  { module:"M2", slug:"pattern-readiness-flap", scenario:"Drill — the readiness probe that flaps", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: a service throws intermittent 502s because its pods cycle in and out of the endpoint pool every few seconds. The readiness probe is wired to a hair trigger — one blip, one second, and the pod is yanked. Tune the probe so it tolerates blips without lying about health.", real:false, writeUp:"pattern-readiness-flap", detail:true },
  { module:"M2", slug:"pattern-zombie-cronjobs", scenario:"Drill — zombie CronJobs pile up", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: a CronJob has been running for weeks with history limits of 50, and dozens of completed Jobs and their pods now clutter the namespace, bloat every LIST, and slow the API server's watchers. Clear the pile and fix the limits so it never regrows.", real:false, writeUp:"pattern-zombie-cronjobs", detail:true },
  { module:"M2", slug:"pattern-rolling-update-deadlock", scenario:"Drill — the rolling update that deadlocks", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: a rollout stuck for hours at \"1 old replicas are pending termination\". maxUnavailable: 0 means no old pod may die until a new one is Ready — and the new one can never be Ready, because its resource request fits on no node. Nothing crashes, nothing progresses. Break the deadlock.", real:false, writeUp:"pattern-rolling-update-deadlock", detail:true },
  { module:"M2", slug:"vpa", scenario:"VPA: the recommender that watched the wrong app", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Right-sizing by hand (like the oomkill lesson) doesn't scale past a few services — the Vertical Pod Autoscaler watches real usage and recommends requests. This cluster's VPA has watched for days and recommended nothing: its targetRef names a Deployment that doesn't exist. Point it at the real workload and read your first recommendation.", real:false, detail:true },
  { module:"M2", slug:"keda-autoscaling", scenario:"KEDA: the ScaledObject that never scaled", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Nightly report traffic needs 3 replicas during business hours; HPA can't scale on \"what time it is\" — KEDA can. But this ScaledObject sits Ready=False and the deployment never leaves 1 replica: the cron trigger's schedule doesn't parse. Fix the trigger, watch KEDA drive the HPA it manages for you.", real:false, detail:true },
  // ── M3 Config & Storage ──────
  { module:"M3", slug:"configmap-wiring", scenario:"CreateContainerConfigError: the key that isn't there", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The `pricing` service won't start — not crashing, not pulling, just CreateContainerConfigError. It wants an env var from a ConfigMap key that doesn't exist. Learn how config wires into containers (env vs mounts) and why the two behave differently when config changes.", real:false, detail:true },
  { module:"M3", slug:"secret-not-mounted", scenario:"ContainerCreating forever: the Secret that isn't", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The `gateway` pod has been ContainerCreating for ten minutes. Its TLS Secret volume references a Secret that doesn't exist under that name. Learn how Secret volumes gate container creation, and the difference between a missing Secret and a missing key.", real:false, detail:true },
  { module:"M3", slug:"pvc-pending", scenario:"PVC Pending: a claim nobody answers", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The analytics database needs a disk. Its PersistentVolumeClaim has been Pending for half an hour — it names a StorageClass that doesn't exist in this cluster. Learn the PV/PVC/StorageClass triangle and how dynamic provisioning actually answers a claim.", real:false, detail:true },
  { module:"M3", slug:"access-modes", scenario:"One disk, two nodes: the access-mode trap", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The wiki scaled to two replicas for \"high availability\" — and the second pod has been Pending ever since. Both want the same ReadWriteOnce volume, and anti-affinity pushes them to different nodes. Learn what access modes really promise and why RWO + spread = deadlock.", real:false, detail:true },
  { module:"M3", slug:"pattern-pvc-terminating", scenario:"Drill — the PVC stuck Terminating", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: a teardown deletes a PersistentVolumeClaim, it goes Terminating… and stays there. Forever. A finalizer is protecting it from a consumer nobody remembers. Find the ghost, release the claim — without the corrupting shortcut.", real:false, writeUp:"pattern-pvc-terminating", detail:true },
  { module:"M3", slug:"kustomize-overlays", scenario:"Kustomize: one base, many environments, zero drift", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Staging and prod were \"the same YAML, copy-pasted\" — until someone hotfixed prod by hand and the copies disagreed. Rebuild the setup as a kustomize base plus a prod overlay with kubectl's built-in -k, and make the drift impossible to express.", real:false, detail:true },
  { module:"M3", slug:"helm-releases", scenario:"Helm: history, rollback, and the failed release", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A Helm upgrade shipped a bad image tag and the release is now `failed` — pods stuck in ImagePullBackOff. Read the release history, roll back to the last good revision, then re-ship the intended change with corrected values. helm install/upgrade/rollback, the full loop.", real:false, detail:true },
  { module:"M3", slug:"pattern-ghost-endpoints", scenario:"Drill — ghost endpoints after scale-down", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: every scale-down or rollout of a service throws a burst of 503s. terminationGracePeriodSeconds is 0 and there is no preStop hook — pods die the instant deletion starts, while their IPs are still in the endpoint list. Give termination the time the network needs.", real:false, writeUp:"pattern-ghost-endpoints", detail:true },
  { module:"M3", slug:"pattern-secret-not-reloaded", scenario:"Drill — the Secret that was rotated but never reloaded", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: security rotated a database password, the Secret shows the new value, the app still authenticates with the old one — because the pod got it as an env var at startup, and env vars never reload. Move the injection to a volume mount and pick up the rotation.", real:false, writeUp:"pattern-secret-not-reloaded", detail:true },
  { module:"M3", slug:"pattern-namespace-terminating", scenario:"Drill — the namespace stuck Terminating", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of a failure pattern reported across many production clusters: a namespace has been Terminating for ten minutes. A custom resource inside it carries a finalizer whose controller was uninstalled long ago — nothing will ever clear it. Find the blocker, and use the finalizer-strip correctly for once: on an object whose owner is permanently gone.", real:false, writeUp:"pattern-namespace-terminating", detail:true },
  // ── M4 Networking ──────
  { module:"M4", slug:"incident-dns-ndots", scenario:"Incident replay — the ndots:5 DNS amplifier (Zalando, Jan 2019)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of a real production outage: Zalando's fashion store lost ALL cluster DNS for over an hour when retry traffic, un-cached lookups and Kubernetes' default ndots:5 multiplied every external hostname lookup into ~10 DNS queries, OOMKilling every CoreDNS pod at once. Find the amplifier in this cluster and defuse it.", real:true, company:"Zalando", source:"https://github.com/zalando-incubator/kubernetes-on-aws/blob/dev/docs/postmortems/jan-2019-dns-outage.md", writeUp:"zalando-dns-outage", detail:true },
  { module:"M4", slug:"networkpolicy-blackhole", scenario:"NetworkPolicy blackhole: default-deny ate my traffic", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Security rolled out a default-deny NetworkPolicy — correct move — but nobody wrote the allow rules that were supposed to ship with it. Learn how policies select, combine, and fail silently, and write the allow rule that lets exactly the right traffic through.", real:false, detail:true },
  { module:"M4", slug:"broken-targetport", scenario:"Connection refused: port vs targetPort", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Endpoints populated, selector correct, pods healthy — and every request gets \"connection refused\". The Service forwards to a container port nobody listens on. Learn the port/targetPort/containerPort chain and the difference between refused and timeout.", real:false, detail:true },
  { module:"M4", slug:"nodeport-vs-clusterip", scenario:"Service types: open a door to the outside", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The demo works inside the cluster, but the client wants to hit it from their laptop. Promote the ClusterIP Service to NodePort on 30080 and understand the ladder: ClusterIP → NodePort → LoadBalancer — what each adds, and what each costs.", real:false, detail:true },
  { module:"M4", slug:"incident-conntrack", scenario:"Incident file — conntrack: the invisible table that fills up", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Read-through of two real, cited incidents (loveholidays, Preply) where the Linux connection-tracking table — a thing most teams don't know exists — silently dropped production traffic and corrupted DNS. No tasks; this is pattern recognition for a failure you can't reproduce safely on a laptop.", real:true, company:"loveholidays", source:"https://deploy.live/blog/kubernetes-networking-problems-due-to-the-conntrack/", writeUp:"conntrack-exhaustion", detail:true },
  { module:"M4", slug:"incident-graceful-shutdown", scenario:"Incident replay — 502s on every deploy (Ravelin's endpoint secret)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of Ravelin's cited write-up: every rolling update produced a burst of 502s, because pod termination and endpoint removal run in PARALLEL, not in order. The fix is a deliberately \"dumb\" preStop sleep. Understand the race, then wire the fix.", real:true, company:"Ravelin", source:"https://philpearl.github.io/post/k8s_ingress/", writeUp:"ravelin-graceful-shutdown", detail:true },
  { module:"M4", slug:"ingress-wiring", scenario:"Ingress: three names that must agree", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The new Ingress went out and the site 404s. An Ingress is pure wiring — host to path to service name to service port to pod — and every hop is a name that must match something real. Two of these names don't. Find them the way you found the selector mismatch: follow the chain.", real:false, detail:true },
  { module:"M4", slug:"gateway-api", scenario:"Gateway API: routing with a role for everyone", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Ingress crams infra and app concerns into one object; Gateway API splits them — GatewayClass for the implementation, Gateway for the listener owned by platform, HTTPRoute for the routes owned by app teams. The platform half is already deployed. Write the app team's HTTPRoute and attach it.", real:false, detail:true },
  { module:"M4", slug:"kube-proxy-dataplane", scenario:"kube-proxy: there is no proxy", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — what actually happens to a packet addressed to a ClusterIP. The IP that exists on no interface, the iptables chains that rewrite it, the IPVS and nftables alternatives, and where conntrack plugs in. Closes the loop on every Service mystery in this module.", real:false, detail:true },
  { module:"M4", slug:"cni-basics", scenario:"CNI: who wires the pod, and what breaks when nobody does", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the container network interface from the kubelet's point of view: the conflist file, the plugin binaries, what Calico and Flannel actually install, and the triage tree for pods stuck ContainerCreating — is it the CNI, or is it a NetworkPolicy?", real:false, detail:true },
  { module:"M4", slug:"kubeconfig-contexts", scenario:"kubeconfig: contexts, the merge, and the prod you almost touched", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A kubeconfig with prod/staging/dev contexts has current-context pointed at prod — one habitual `kubectl apply` away from a bad day. Switch to staging, then merge a teammate's kubeconfig into one flattened file without clobbering either. kubectl config end to end.", real:false, detail:true },
  // ── M5 Scheduling & Placement ──────
  { module:"M5", slug:"incident-same-node", scenario:"Incident replay — all replicas on the failing node (Moonlight)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of Moonlight's cited outage: every pod of the website landed on the same host — the one that then failed. 100% traffic loss with \"redundant\" replicas. Spread them so one node can never again take the whole service down.", real:true, company:"Moonlight", source:"https://updates.moonlightwork.com/outage-post-mortem-87370", writeUp:"moonlight-same-node", detail:true },
  { module:"M5", slug:"taints-tolerations", scenario:"Everything Pending: who tainted the nodes?", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Overnight, every new pod in the cluster stopped scheduling. Someone prepared the workers for a \"dedicated batch pool\" migration that was cancelled — but the taints stayed. Learn how taints repel, how tolerations permit, and how to read the scheduler's rejection notes.", real:false, detail:true },
  { module:"M5", slug:"topology-spread", scenario:"Topology spread: balance, not just separation", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Six replicas of the session store — five on one node, one on another. Anti-affinity can't fix this: it only says \"not together\", not \"evenly\". Learn topologySpreadConstraints, maxSkew math, and when soft beats hard.", real:false, detail:true },
  { module:"M5", slug:"incident-priority-preemption", scenario:"Incident replay — the priority that ate production (Grafana Labs)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of Grafana Labs' cited outage: introducing PriorityClasses with the wrong defaults caused Kubernetes to PREEMPT — evict — production pods to make room. Set the priority tiers right so the important thing wins the next resource fight.", real:true, company:"Grafana Labs", source:"https://grafana.com/blog/2019/07/24/how-a-production-outage-was-caused-using-kubernetes-pod-priorities/", writeUp:"grafana-priority-preemption", detail:true },
  { module:"M5", slug:"pattern-noisy-neighbor", scenario:"Drill — the noisy neighbor", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Synthetic drill of the fleet-wide classic: one workload declares no resources and feasts, its neighbor is CPU-throttled into 2-second latency. Nothing crashes; everything is slow. Diagnose with resource math, fix with honest declarations.", real:false, detail:true },
  // ── M6 Security ──────
  { module:"M6", slug:"rbac-least-privilege", scenario:"cluster-admin for a bot: scope it down", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The CI bot needs to read pods in one namespace. Someone gave it cluster-admin — write access to everything, everywhere, forever. Replace the god-grant with a least-privilege Role and prove the difference with kubectl auth can-i.", real:false, detail:true },
  { module:"M6", slug:"incident-cryptominer", scenario:"Incident replay — the exposed dashboard (JW Player's cryptominer)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of JW Player's cited incident: an internal ops tool got a public LoadBalancer with no auth, and attackers used it to launch a cryptominer across the cluster. Find the pod nobody deployed, kill it, and shut the door it walked through.", real:true, company:"JW Player", source:"https://medium.com/jw-player-engineering/how-a-cryptocurrency-miner-made-its-way-onto-our-internal-kubernetes-clusters-9b09c4704205", writeUp:"jwplayer-cryptominer", detail:true },
  { module:"M6", slug:"incident-webhook-outage", scenario:"Incident replay — the webhook that froze the cluster (Jetstack)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of Jetstack's cited GKE outage: a validating webhook with failurePolicy=Fail lost its backing pods, so the API server rejected EVERY write — including the ones needed to recover. Fix the failure policy and scope so a webhook can never again take the whole cluster hostage.", real:true, company:"Jetstack", source:"https://blog.jetstack.io/blog/gke-webhook-outage", writeUp:"jetstack-webhook-outage", detail:true },
  { module:"M6", slug:"pod-security-baseline", scenario:"Pod Security: the privileged pod that shouldn't exist", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A vendor Helm chart wants a privileged, host-network pod with hostPath to the Docker socket — a full container escape, one YAML away from root on the node. Enforce Pod Security Standards on the namespace and rework the pod to run unprivileged.", real:false, detail:true },
  { module:"M6", slug:"serviceaccount-tokens", scenario:"The token in every pod (and who's using yours)", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Every pod in the namespace is carrying an API credential nobody asked for — the default ServiceAccount token, automounted since day one. Give the one workload that needs API access its own identity, and take the credential away from everything that doesn't.", real:false, detail:true },
  { module:"M6", slug:"container-hardening", scenario:"Hardening: take away everything the workload doesn't use", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The worker runs as root with a writable filesystem, every Linux capability, and no syscall filter — and uses none of it. Strip it to what it actually needs: non-root, read-only root filesystem, zero capabilities, RuntimeDefault seccomp. The pod should barely notice; an attacker inside it definitely will.", real:false, detail:true },
  { module:"M6", slug:"cis-kube-bench", scenario:"Audit the cluster: CIS benchmark with kube-bench", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"You hardened pods; now audit the platform itself. Run kube-bench — the CIS Kubernetes Benchmark as a Job — against this cluster, read its PASS/FAIL/WARN verdicts on the API server, kubelet, and etcd configuration, and learn which findings matter before an auditor (or an attacker) reads them to you.", real:false, detail:true },
  { module:"M6", slug:"control-plane-hardening", scenario:"The hardening you can't kubectl: control plane, supply chain, runtime", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the CKS-shaped half of security that lives outside the pod spec: encrypting Secrets at rest, audit logging, API server flags, image supply chain (digests, scanning, signing), and runtime security (Falco, gVisor, AppArmor). Where each control lives, what it stops, and who owns it.", real:false, detail:true },
  { module:"M6", slug:"egress-lockdown", scenario:"Egress lockdown: the miner needs a phone line", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The cryptominer from lesson 6.2 did its damage over outbound connections — and outbound is the direction nobody polices. Lock the payments namespace down: default-deny all egress, then allow back exactly two flows — DNS, and the payment gateway. Everything else, including the next miner, dials a dead line.", real:false, detail:true },
  { module:"M6", slug:"image-digests", scenario:"Tags lie, digests don't: pin the supply chain", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A tag like nginx:1.27-alpine is a mutable pointer — whoever controls the registry controls what it points at, and yesterday's audited image may not be tomorrow's. Find the digest of what checkout is actually running and pin the deployment to it, making the image content-addressed and unswappable.", real:false, detail:true },
  { module:"M6", slug:"opa-gatekeeper", scenario:"Gatekeeper: the policy that rejected nothing", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"OPA Gatekeeper is installed and a Constraint says \"no :latest tags\" — yet nginx:latest sails right in. The Rego in the ConstraintTemplate reads a field that doesn't exist on a Pod, so the violation never fires. Fix the policy so it actually denies, without denying everything else.", real:false, detail:true },
  { module:"M6", slug:"kyverno-policies", scenario:"Kyverno: the policy that blocked kube-system", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A Kyverno ClusterPolicy in Enforce mode requires an `app` label on every pod — in every namespace, every team, cluster-wide. Only Kyverno's default resourceFilters kept it from bricking kube-system too. Scope the policy to where it belongs, add a mutation that fixes pods instead of rejecting them, and keep one hard deny.", real:false, detail:true },
  { module:"M6", slug:"image-scanning-pipeline", scenario:"Scan it, then pin it: trivy and the digest", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A Deployment is running nginx:1.14 — an image with a museum's worth of known CVEs — referenced by a mutable tag on top. Scan it with trivy, find a clean replacement, and pin the fix by digest so nobody can swap the bytes behind your back.", real:false, detail:true },
  { module:"M6", slug:"sbom-cosign", scenario:"Signatures and SBOMs: trust, but verify with cosign", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Reading with a hands-on core — what image signing actually proves, how Sigstore keyless signatures work, and what an SBOM buys you. Then do it for real: cosign-verify a publicly signed image and record the digest the signature vouches for.", real:false, detail:true },
  { module:"M6", slug:"apparmor-seccomp", scenario:"seccomp on, AppArmor understood", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A pod is running with an unfiltered syscall surface — all ~350 syscalls exposed to whatever escapes the app. Turn on the runtime's default seccomp profile the Kubernetes way, then read the AppArmor runbook for the host-side twin you'll meet on real nodes and the CKS.", real:false, detail:true },
  { module:"M6", slug:"encryption-at-rest", scenario:"Encryption at rest: the full runbook", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the complete EncryptionConfiguration runbook the hardening survey only gestured at: provider order semantics, aescbc vs KMS v2, the apiserver wiring, proving ciphertext with etcdctl, the re-encrypt migration, and key rotation without locking yourself out.", real:false, detail:true },
  { module:"M6", slug:"audit-policy", scenario:"Audit policy: who touched that Secret?", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the audit-policy runbook behind survey §2: the four levels and what each actually records, a production-shaped policy that captures Secret reads and pod exec without drowning in kubelet noise, the apiserver wiring, and how to interrogate the log when it matters.", real:false, detail:true },
  { module:"M6", slug:"falco-runtime-detection", scenario:"Falco: alarms for the attack you didn't prevent", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — runtime detection with Falco behind survey §5: how the eBPF probe sees every syscall, the rules language, writing the shell-in-a- container rule from scratch, the install runbook, and routing alerts so a human actually sees them.", real:false, detail:true },
  // ── M7 Internals ──────
  { module:"M7", slug:"reconcile-loop", scenario:"The reconcile loop: why deleted pods come back", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Delete a pod and it reappears in seconds — not magic, but a controller comparing desired state to actual and acting on the difference. Watch the ReplicaSet controller heal a Deployment in real time, and learn the single idea the whole control plane is built on.", real:false, detail:true },
  { module:"M7", slug:"scheduler-nodename", scenario:"What the scheduler actually does (bypass it to find out)", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"The scheduler feels like a black box. It isn't — its entire job is to write one field: spec.nodeName. Prove it by scheduling a pod yourself, bypassing the scheduler entirely, then reason about filter-and-score placement.", real:false, detail:true },
  { module:"M7", slug:"etcd-truth", scenario:"etcd: the one source of truth", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Every object you have ever created lives as a key in etcd. Write a ConfigMap, then find it byte-for-byte inside etcd at /registry/... — and understand why \"the API server is stateless\" and \"back up etcd or lose everything\" are the same fact.", real:false, detail:true },
  { module:"M7", slug:"control-plane-tour", scenario:"The control-plane tour: request flow, kubelet, leases", type:"read", iximiuz:false, kind:false, handsOn:false, description:"A guided walk through the machinery — what happens between \"kubectl apply\" and a running container, how the kubelet turns a PodSpec into containers via the CRI, and how leader election keeps one controller-manager in charge. Reading + live pokes on this cluster; no single check to pass.", real:false, detail:true },
  { module:"M7", slug:"crd-operators", scenario:"CRDs: teach the API server a new noun", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A team shipped manifests for a BackupSchedule resource — and the apply fails with \"no matches for kind\". Nothing is broken: the cluster just doesn't know that noun yet. Install the CustomResourceDefinition, watch the API grow a new endpoint, and see how CRD + controller = operator.", real:false, detail:true },
  { module:"M7", slug:"etcd-backup-restore", scenario:"etcd backup & restore: the runbook you rehearse before you need it", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the etcdctl snapshot save/restore runbook, step by step: what a snapshot contains, the restore's new-cluster semantics, the cert flags everyone fumbles, and why Reddit's Pi-Day recovery took five hours. The last lesson of \"etcd is the cluster\": knowing how to put it back.", real:false, detail:true },
  { module:"M7", slug:"admission-mutations", scenario:"Admission: the API server edits your YAML before storing it", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Apply a bare pod and read back what the cluster stored: resources you never wrote, a ServiceAccount you never named, volumes you never mounted. That's the admission chain rewriting objects in flight. Drive it yourself with a LimitRange — defaults in (mutation), oversized pods out (validation).", real:false, detail:true },
  { module:"M7", slug:"watch-informers", scenario:"Watch: how every controller hears the cluster", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading with live experiments — the list+watch mechanism underneath every controller, kubectl -w, and the scheduler alike: resourceVersion as a cursor, watch events as a change feed, informers as the cache that makes 10,000 controllers cheap, and API Priority & Fairness as the API server's self-defense when they aren't.", real:false, detail:true },
  { module:"M7", slug:"build-an-operator", scenario:"Build an operator: the verb for your noun", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the capstone for internals. You created the CRD (7.5); now walk the controller that would bring it to life: informer, workqueue, reconcile(key), status writes, ownerReferences. Annotated minimal code, the crash-safety argument, and the honest ladder from client-go to kubebuilder — including when not to write an operator at all.", real:false, detail:true },
  { module:"M7", slug:"kubeadm-bootstrap", scenario:"kubeadm: a cluster from three commands", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the full kubeadm init → join runbook: what each phase actually does, the pod-network-cidr decision you can't take back, the kubeconfig copy, join tokens and their expiry, the CNI step everyone forgets, and how to read a bootstrap that went sideways.", real:false, detail:true },
  { module:"M7", slug:"ha-control-plane", scenario:"HA control plane: three of everything", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — what it takes for the control plane to survive losing a node: stacked vs external etcd, the --control-plane-endpoint decision, joining more control-plane nodes, who load-balances the apiserver, and how the singleton controllers pick a leader.", real:false, detail:true },
  { module:"M7", slug:"cert-rotation", scenario:"Certificates: the outage scheduled a year in advance", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — kubeadm clusters mint one-year certificates, and expired certs are a whole-cluster lockout that arrives on schedule. The check-expiration and renew runbook, which components restart, CA vs leaf lifetimes, kubelet auto-rotation, and the x509 error you'll otherwise meet at 3 a.m.", real:false, detail:true },
  // ── M8 Observability & SRE ──────
  { module:"M8", slug:"events-forensics", scenario:"Events forensics: reconstruct the crime", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A pod is gone and nobody knows why. No logs survive a deleted pod — but the cluster's events remember. Learn to read events as a timeline, the tool that turns \"it just disappeared\" into a precise cause.", real:false, detail:true },
  { module:"M8", slug:"incident-node-oom", scenario:"Incident replay — the node that OOMed itself (Blue Matador)", type:"incident", iximiuz:true, kind:true, handsOn:true, description:"Replay of Blue Matador's cited postmortem: pods with no memory limits grew until the NODE ran out, and the kernel OOM-killer started shooting processes — including system daemons — at random. Learn node-level OOM vs pod OOM, and cap the workload before the kernel does it for you.", real:true, company:"Blue Matador", source:"https://www.bluematador.com/blog/post-mortem-kubernetes-node-oom", writeUp:"bluematador-node-oom", detail:true },
  { module:"M8", slug:"quota-exhausted", scenario:"Deploy blocked: the quota nobody mentioned", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A scale-up half-worked: 2 of 5 pods created, then FailedCreate, and the Deployment is stuck. A ResourceQuota is out of room — and the error is on the ReplicaSet, not the pods. Learn to read quota, and the layer where this class of failure hides.", real:false, detail:true },
  { module:"M8", slug:"node-notready", scenario:"The morning after NotReady", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A worker went NotReady overnight — kubelet certificate expired, on-call fixed it at 3 a.m. and went back to bed. The node is healthy now, but half your capacity is still gone and pods are Pending. Learn what NotReady actually does to a node — taints, evictions, cordons — and how to clean up after it.", real:false, detail:true },
  { module:"M8", slug:"pattern-disk-pressure", scenario:"Pattern drill: evicted — the disk you forgot to budget", type:"drill", iximiuz:true, kind:true, handsOn:true, description:"Pods keep dying with a status you haven't seen before: Evicted. No crash, no OOM, exit code nowhere — the kubelet itself killed them for eating too much disk. Ephemeral storage is the resource nobody puts in their manifests, and eviction is how you find out.", real:false, detail:true },
  { module:"M8", slug:"incident-datadog-cilium", scenario:"Incident file — the OS under the cluster (Datadog, 2023)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of Datadog's cited 2023 outage: a routine systemd security update auto-applied across the fleet restarted systemd-networkd, which silently deleted the network routes Cilium had installed — and tens of thousands of nodes across five regions dropped off the network in the same hour. The layer below Kubernetes can take down everything above it.", real:true, company:"Datadog", source:"https://www.datadoghq.com/blog/2023-03-08-multiregion-infrastructure-connectivity-issue/", writeUp:"datadog-cilium-routes", detail:true },
  { module:"M8", slug:"upgrade-runbook", scenario:"The upgrade runbook: version skew, kubeadm, and not becoming a war story", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — how production Kubernetes clusters are upgraded without downtime: the version-skew contract that makes rolling upgrades possible, the kubeadm sequence (plan, apply, drain, uncordon, repeat), what breaks anyway, and the pre-flight checklist distilled from the incidents in this course.", real:false, detail:true },
  { module:"M8", slug:"node-maintenance", scenario:"Node maintenance: drain like you mean it", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A worker node needs a kernel patch. It runs a DaemonSet pod and a batch pod with emptyDir scratch data — the two things `kubectl drain` refuses to touch without explicit flags. Run the full maintenance cycle: cordon, drain past both refusals, \"patch\", uncordon, and leave a paper trail.", real:false, detail:true },
  { module:"M8", slug:"slo-errorbudget", scenario:"SLOs: the burn-rate alert that never fired", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"An SLO burn-rate alert has been silently broken for months: its PrometheusRule references a metric that doesn't exist, and the \"error ratio\" counts 404s as server errors. Fix both bugs and prove the burn rate evaluates against live traffic.", real:false, detail:true },
  { module:"M8", slug:"otel-collector-pipeline", scenario:"OTel pipeline: traces into the void", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"An OpenTelemetry Collector receives traces from every service and exports them to Jaeger — except Jaeger has been empty for a week. The collector's exporter points at a service name that doesn't exist, and it retries into the void. Read the pipeline config, fix the endpoint, watch traces land.", real:false, detail:true },
  // ── M9 War Stories ──────
  { module:"M9", slug:"incident-monzo-cascade", scenario:"Incident file — the cascade (Monzo's bank-stopping outage)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of Monzo's cited outage: a routine change touched etcd, which confused a service mesh, which returned empty endpoints, which crashed clients on a null pointer — and a bank's payments stopped. Trace how five familiar mechanisms compose into one national-scale failure.", real:true, company:"Monzo", source:"https://community.monzo.com/t/resolved-current-account-payments-may-fail-major-outage-27-10-2017/26296/95", writeUp:"monzo-cascade", detail:true },
  { module:"M9", slug:"incident-openai-cascade", scenario:"Incident file — locked out of the control plane (OpenAI, 2024)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of OpenAI's cited December 2024 outage: a fleet-wide telemetry rollout overwhelmed every Kubernetes API server at once, DNS caching hid the damage until the rollout was everywhere, and the overloaded control plane locked engineers out of the fix. Control plane vs data plane, as a war story.", real:true, company:"OpenAI", source:"https://status.openai.com/incidents/ctrsv3lwd797", writeUp:"openai-telemetry-cascade", detail:true },
  { module:"M9", slug:"incident-reddit-piday", scenario:"Incident file — the Pi-Day outage (Reddit, 2023)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of Reddit's cited Pi-Day outage: a routine Kubernetes upgrade on their oldest cluster renamed one node label — and the CNI's route reflectors, selecting nodes by the old name, went empty. Pod networking collapsed cluster-wide. A war story about deprecations, snowflake clusters, and why restores get tested before they're needed.", real:true, company:"Reddit", source:"https://www.reddit.com/r/RedditEng/comments/11xx5o0/you_broke_reddit_the_piday_outage/", writeUp:"reddit-piday", detail:true },
  { module:"M9", slug:"incident-black-friday", scenario:"Incident file — killing the dashboard on Black Friday (Algolia)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of Algolia's cited war story: a Jobs-based workload meets the biggest traffic day of the year, work piles up faster than it drains, and the accumulated load takes the platform down at peak. The overload playbook — backpressure, cleanup, and capacity math — as a story with a date on it.", real:true, company:"Algolia", source:"https://www.youtube.com/watch?v=Fjyg7cxRZQs", writeUp:"algolia-black-friday", detail:true },
  { module:"M9", slug:"incident-target-cascade", scenario:"Incident file — the five-system cascade (Target, 2019)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of Target's cited cascading failure: an OpenStack network upgrade made Kafka flap, logging sidecars stampeded, Docker daemons buckled, Kubernetes rescheduling poisoned Consul's gossip mesh with 41,000 phantom nodes, and Vault sealed itself. A war story about cascades that refuse to stay inside one system's boundary.", real:true, company:"Target", source:"https://medium.com/@daniel.p.woods/on-infrastructure-at-scale-a-cascading-failure-of-distributed-systems-7cff2a3cd2df", writeUp:"target-cascade", detail:true },
  { module:"M9", slug:"incident-spotify-delete", scenario:"Incident file — deleting every cluster, twice (Spotify, 2019)", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Guided study of Spotify's cited KubeCon keynote: during the GKE migration, production Kubernetes clusters were accidentally deleted — twice — with little to no user impact. The war story where the interesting question is why nothing happened, and what that says about where cluster state should live.", real:true, company:"Spotify", source:"https://www.youtube.com/watch?v=ix0Tw8uinWs", writeUp:"spotify-delete", detail:true },
  { module:"M9", slug:"final-boss", scenario:"Final boss: three faults, no hints", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A checkout stack with three independent faults stacked on top of each other — a selector mismatch, a bad probe, and an impossible resource request. No hints, no fault list. Diagnose each from first principles and ship the fix. This is the exam.", real:false, detail:true },
  // ── M10 Platform Engineering ──────
  { module:"M10", slug:"gitops-argocd", scenario:"Argo CD: the app that refuses to sync", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Argo CD is installed and an Application points at the guestbook example — but the repo path in its spec doesn't exist, so it can't even compare desired vs live. Read the Application status like an operator, fix the source, and watch auto-sync converge it to Synced/Healthy.", real:false, detail:true },
  { module:"M10", slug:"gitops-argocd-appofapps", scenario:"App of apps: one bad child in the fleet", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Three Applications deploy the shop's frontend stack in sync-wave order — and the fleet is stuck: one child pins its targetRevision to a branch that doesn't exist in the repo. Triage a multi-app rollout, fix the bad revision, and get every Application to Synced/Healthy.", real:false, detail:true },
  { module:"M10", slug:"gitops-flux2", scenario:"Flux: the Kustomization that can't find its source", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Flux reconciles podinfo from git — except the GitRepository pins a branch that doesn't exist, so the artifact never materializes and the Kustomization downstream starves. Read Flux's condition chain source → kustomization, fix the ref, watch it converge.", real:false, detail:true },
  { module:"M10", slug:"multi-tenancy-capsule", scenario:"Capsule: the tenant that hit its walls", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"Multi-tenancy without a cluster per team: Capsule carves one cluster into tenants. team-alpha was sized for a single namespace back when it was a single service — now every attempt to add one bounces off the tenant quota. Resize the tenant, prove the walls still hold between teams.", real:false, detail:true },
  { module:"M10", slug:"cluster-api-intro", scenario:"Cluster API: clusters as custom resources", type:"read", iximiuz:false, kind:false, handsOn:false, description:"Reading — the endgame of the operator pattern: a management cluster whose CRDs describe entire Kubernetes clusters, with providers reconciling machines the way a Deployment reconciles pods. Architecture, the core objects, a real MachineDeployment, and when CAPI beats kubeadm-by-hand.", real:false, detail:true },
  { module:"M10", slug:"crossplane-compositions", scenario:"Crossplane: the composition missing its provider", type:"lab", iximiuz:true, kind:true, handsOn:true, description:"A platform team publishes XDatabase as self-service API — claim one and a Composition assembles the real resources. Except every XDatabase sits Synced=False: the Composition emits a resource kind no installed provider understands. Read the composition error, install the missing provider, watch the claim go Ready.", real:false, detail:true },
];
