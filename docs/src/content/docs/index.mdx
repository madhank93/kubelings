---
title: Kubelings
description: Hands-on Kubernetes lessons — fix broken-on-purpose clusters until an automated check passes. For CKA/CKAD/CKS prep and production troubleshooting muscle memory.
template: splash
hero:
  tagline: Fix broken-on-purpose clusters until an automated check passes — the rustlings way. For engineers building real Kubernetes muscle memory, from first pod to node and control-plane failures below kubectl.
  image:
    file: ../../assets/kubelings.png
    alt: Kubelings
  actions:
    - text: iximiuz Labs
      link: https://labs.iximiuz.com/courses/kubelings-dbd840c8
      icon: external
      variant: primary
    - text: Catalog
      link: /catalog/
      icon: right-arrow
      variant: secondary
      attrs:
        class: cta-catalog
    - text: GitHub
      link: https://github.com/madhank93/kubelings
      icon: external
      variant: secondary
      attrs:
        class: cta-github
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import AsciinemaPlayer from '../../components/AsciinemaPlayer.astro';
import { CATALOG, MODULES } from '../../data/catalog';

export const moduleIds = Object.keys(MODULES);
export const lessonCount = CATALOG.length;
export const incidentCount = CATALOG.filter((l) => l.real).length;
export const lessonsIn = (id) => CATALOG.filter((l) => l.module === id).length;
export const MODULE_BLURBS = {
  M1: 'pods, Deployments, Services, the triage loop',
  M2: 'HPA, StatefulSets, PDBs, OOMKill, probes, VPA, KEDA',
  M3: 'ConfigMaps, Secrets, PV/PVC, Helm, Kustomize',
  M4: 'Ingress, Gateway API, NetworkPolicy, CoreDNS, CNI',
  M5: 'affinity, taints, topology spread, preemption',
  M6: 'RBAC, Pod Security, Gatekeeper, Kyverno, Falco, cosign',
  M7: 'API server, etcd, scheduler, CRDs, operators, HA',
  M8: 'SLO burn-rate, OTel tracing, node triage, upgrades',
  M9: 'multi-concept cascades from real postmortems',
  M10: 'Argo CD, Flux, Capsule, Cluster API, Crossplane',
  M11: 'kubelet/containerd/etcd/apiserver on real hosts — iximiuz Labs only',
};

<AsciinemaPlayer caption="A broken cluster in your terminal: filter to a lesson, inject the broken state into a live kind cluster, and let the automated check decide — it only passes once the cluster is genuinely fixed." />


<ul class="stat-row">
  <li><span class="stat-num">{lessonCount}</span><span class="stat-label">Lessons</span></li>
  <li><span class="stat-num">{moduleIds.length}</span><span class="stat-label">Modules</span></li>
  <li><span class="stat-num">{incidentCount}</span><span class="stat-label">Real incidents</span></li>
</ul>

## What is Kubelings?

**Kubelings** is hands-on Kubernetes practice in the spirit of *rustlings* and
*golings*: a sequence of small, broken-on-purpose clusters, each with an automated
check that only passes when you've genuinely fixed the problem.

**Best for** engineers preparing for **CKA / CKAD / CKS**, or anyone who wants
Kubernetes reflexes — not just theory. Beginner → advanced, free, and it runs the
same on a browser VM or your own laptop.

## Two ways to run

<CardGrid>
  <Card title="☁ No setup — fastest" icon="rocket">
    Run instantly in your browser on
    [iximiuz Labs](https://labs.iximiuz.com/courses/kubelings-dbd840c8) — zero
    install, real clusters, nothing to clean up. Best way to start.
  </Card>
  <Card title="💻 Local — offline / air-gap" icon="laptop">
    Clone the repo and run on `kind` with the TUI or CLI. See
    [Getting Started](/getting-started/). One source of truth drives both — the
    checks are identical.
  </Card>
</CardGrid>

## The curriculum, module by module

From first pod to control-plane internals, platform engineering, and the
node/host failures below kubectl — with real, cited production incidents
reproduced as runnable lessons along the way.

<table>
  <thead>
    <tr><th>#</th><th>Module</th><th>You learn</th><th>Lessons</th></tr>
  </thead>
  <tbody>
    {moduleIds.map((id, i) => (
      <tr>
        <td>{String(i + 1).padStart(2, '0')}</td>
        <td><strong>{MODULES[id].label}</strong></td>
        <td>{MODULE_BLURBS[id]}</td>
        <td>{lessonsIn(id)}</td>
      </tr>
    ))}
  </tbody>
</table>

[Browse every lesson, with skill outcomes and the incident write-ups →](/catalog/)

Every lesson is one of four types — three **hands-on** (you fix a live cluster,
verify-gated) and one **read-only**:

<ul class="kind-legend">
  <li><span class="kind-tag lab">lab</span> hands-on concept lesson</li>
  <li><span class="kind-tag incident">incident</span> hands-on real production incident, reproduced</li>
  <li><span class="kind-tag drill">drill</span> hands-on synthetic failure pattern</li>
  <li><span class="kind-tag read">read</span> read-only runbook, no tasks</li>
</ul>

## What makes it different

<CardGrid>
  <Card title="Interactive Go TUI" icon="laptop">
    Press `↵` on a lesson — it spins up a 3-node `kind` cluster, injects the
    broken state, and drops you into a shell. Progress markers per lesson, no YAML
    editing to start. See [The TUI](/guides/tui/).
  </Card>
  <Card title="Real incidents, reproduced" icon="random">
    {incidentCount} cited production postmortems — Zalando, Reddit, OpenAI, Monzo, Grafana —
    reproduced as runnable lessons on the same broken state the original team
    faced. Real vs synthetic is labeled honestly. Browse them in the
    [Catalog](/catalog/).
  </Card>
  <Card title="Check-driven" icon="approve-check">
    Each lesson ships `init` + `verify` tasks; the check turns green only when the
    cluster is genuinely in the target state — no self-grading.
  </Card>
  <Card title="Hardened by design" icon="approve-check-circle">
    `init`/`verify` scripts run in a confined context — they can't reach outside
    the cluster. Your host filesystem, credentials, and network are never touched.
    See [Security](/reference/security/).
  </Card>
</CardGrid>

---

## About the author

Kubelings is created and maintained by **Madhan Kumaravelu**
([@madhank93](https://github.com/madhank93) · [madhan.app](https://madhan.app)) —
the {lessonCount} lessons, the incident library, the TUI, and the engine that runs every
scenario both on iximiuz Labs and on local `kind`. Found a bug or want a lesson
added? [Open an issue](https://github.com/madhank93/kubelings/issues).
